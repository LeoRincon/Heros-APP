import React from 'react';
import ReactDOM from 'react-dom';
import { HerosApp } from './containers/HerosApp';

ReactDOM.render(
  <React.StrictMode>
    <HerosApp />
  </React.StrictMode>,
  document.getElementById('root')
);
